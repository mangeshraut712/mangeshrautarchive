# Implementation Plans

Generated by the **improve** skill on **2026-07-12** against commit `915866f2`.

Execute in the order below unless dependencies say otherwise. Each executor: read the plan fully before starting, honor STOP conditions, and update your row when done.

**Verification baseline (recon):** `npm audit --omit=dev` → 0 high+; `npm run check` → pass; `npm run test:api` → 109 passed. Runtime: Node 22 · Python 3.12 · vanilla ES modules + FastAPI.

## Execution order & status

| Plan | Title                                                               | Priority | Effort | Depends on | Status |
| ---- | ------------------------------------------------------------------- | -------- | ------ | ---------- | ------ |
| 001  | Fix AssistMe host detection, stream timeout, and server-AI recovery | P1       | S      | —          | DONE   |
| 002  | Fail-closed Google Calendar webhook verification                    | P1       | S      | —          | DONE   |
| 003  | Trust platform client IP for rate limits (ignore spoofed XFF)       | P1       | S      | —          | DONE   |
| 004  | Dual-host API base for newsletter (and shared helper)               | P1       | S      | —          | DONE   |
| 005  | Harden Last.fm artwork URLs (no raw URL in innerHTML)               | P1       | S      | —          | DONE   |

Status values: `TODO` | `IN PROGRESS` | `DONE` | `BLOCKED` | `REJECTED`

## Dependency notes

- 001–005 are independent; can parallelize if separate branches, otherwise land 001 first (highest user-visible AI impact).
- Security plans 002–003–005 do not depend on each other.
- Larger follow-ups (durable rate limits, GitHub proxy allowlist, CSP nonces, PII split from system prompt, CSS consolidation) are **not planned yet** — see audit table in the advisor report; promote only if selected.

## Findings considered and rejected (this run)

- **iOS multi-WebGL crash loop**: Already mitigated (`isLowPowerClient` / iOS WebGL disable path) — not re-planned.
- **Deploy version reload loops**: Hard-capped in `bootstrap.js` — not re-planned.
- **God-module split of chatbot/bootstrap**: High risk without characterization tests first — deferred after TEST-01 style work.
- **Full CSS mega-bundle (PERF-01)**: Large cascade risk; prefer generator CSS alignment (PERF-02) as a later plan if selected.
- **Hard-fail CI when Vercel lags Pages**: Would flake on Vercel queue; keep soft-fail + nightly parity for now.

## Not audited in depth

- OAuth token vault / Supabase RLS policies (integration layer only surface-scanned)
- Full Playwright multi-browser matrix (not re-run this session)
- Visual regression of liquid-glass on every subpage
- Third-party CDN integrity (jsDelivr / Font Awesome vendor)
