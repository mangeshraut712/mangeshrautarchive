version: '3.8'

services:
  api-monitor:
    build: .
    ports:
      - '3001:3001'
    environment:
      - NODE_ENV=production
    env_file:
      - .env
    volumes:
      - ./logs:/app/logs
    restart: unless-stopped
